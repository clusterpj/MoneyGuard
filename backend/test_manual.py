import requests
import json

BASE_URL = "http://localhost:8000/api/v1"

def test_expenses():
    # This is a manual script to be run if the server is up.
    # For now, I'm just creating it to document how I would test it.
    pass
